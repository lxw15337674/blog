# 接口
用于为类型指定规则。
## 简单例子
```js
function getName(person:{name:string}){
    console.log(person.name)
}
```
等价于
```js
interface Person{
    name:string
}
function getName(person:Person){
    console.log(person.name)
}
```
